import Vue from 'vue'
import Vuex from 'vuex'

import screens from "@/store/modules/screens";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    screens,
  }
})
